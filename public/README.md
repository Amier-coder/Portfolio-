# Portfolio

This folder holds static assets served from the site root.

- `resume.pdf` — drop your CV here (the "Download CV" button links to `/resume.pdf`).
- `profile.jpg` — optional profile photo. If you add it, set `profile.photo = "/profile.jpg"` in `src/data/portfolio.ts`.
